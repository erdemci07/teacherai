import type { GraphData } from '../lib/lesson-api';
export function GraphView({data}:{data:GraphData}){try{const points=data.sample_points.filter(([,y])=>Number.isFinite(y)).map(([x,y])=>`${100+x*25},${100-y*18}`).join(' ');if(!points)return <p role="note">Grafik bu soru için çizilemedi.</p>;return <svg className="boardGraph" viewBox="0 0 200 200" role="img" aria-label={`${data.expression} fonksiyon grafiği`}><line x1="10" y1="100" x2="190" y2="100"/><line x1="100" y1="10" x2="100" y2="190"/><polyline points={points}/></svg>}catch{return <p role="note">Grafik bu soru için çizilemedi.</p>}}
