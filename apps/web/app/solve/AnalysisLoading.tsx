'use client';
import {useEffect,useState} from 'react';
const stages:Record<string,string[]>={uploading:['Soruyu hazırlıyorum...'],analyzing:['Soruyu okuyorum...','Verilenleri inceliyorum...','Hangi konudan olduğunu belirliyorum...'],planning:['Çözüm yolunu hazırlıyorum...','Matematiksel kontrolü yapıyorum...','Anlatımı hazırlıyorum...'],verifying:['Matematiksel kontrolü yapıyorum...'],rendering:['Anlatımı hazırlıyorum...']};
export function AnalysisLoading({uploading,stage}:{uploading:boolean;stage?:string}){const key=uploading?'uploading':stage??'analyzing',[index,setIndex]=useState(0),messages=stages[key]??stages.analyzing;useEffect(()=>{setIndex(0);if(messages.length<2)return;const timer=window.setInterval(()=>setIndex(value=>(value+1)%messages.length),2200);return()=>window.clearInterval(timer)},[key,messages.length]);return <div className="analysisLoading" role="status" aria-live="polite"><span className="loadingSpinner" aria-hidden="true"/><div><strong>{messages[index]}</strong><p>Birazdan öğretmen anlatımın hazır olacak.</p></div></div>}
