import type {LessonContent} from '../lib/lesson-api';import {MathExpression} from './MathExpression';import {RichMathText} from './RichMathText';
export function LessonText({lesson}:{lesson:{content:LessonContent}}){const c=lesson.content;return <div className="lessonText"><h2>Metin anlatımı</h2><p><RichMathText text={c.question_understanding}/></p>{c.prerequisite_reminder&&<p><strong>Hatırlatma:</strong> <RichMathText text={c.prerequisite_reminder}/></p>}{c.key_rule&&<p><strong>Kural:</strong> <RichMathText text={c.key_rule}/></p>}<p><strong>Strateji:</strong> <RichMathText text={c.strategy}/></p>{c.steps.map(x=><section key={x.id}><h3>{x.title}</h3><p><RichMathText text={x.explanation}/></p>{x.expressions.map((e,i)=><MathExpression latex={e.latex} key={i}/>)}</section>)}{c.common_mistake&&<p><strong>Sık hata:</strong> <RichMathText text={c.common_mistake}/></p>}{c.shortcut&&<p><strong>Kısa yol:</strong> <RichMathText text={c.shortcut}/></p>}{c.teacher_tip&&<p><strong>Öğretmen notu:</strong> <RichMathText text={c.teacher_tip}/></p>}<h3>Cevap</h3><p><RichMathText text={c.final_answer}/></p>{c.final_answer_expressions.map((e,i)=><MathExpression latex={e.latex} key={`final-${i}`}/>)}<p><strong>Akılda kalsın:</strong> <RichMathText text={c.takeaway}/></p></div>}
